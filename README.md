# Recipe Pantry Planner

A static, GitHub Pages-friendly recipe browser maintained from R-readable files.

## Workflow

1. Add or edit recipe Markdown files in `recipes/`.
2. Edit pantry items in `data/pantry.csv`.
3. Add dates cooked in each recipe's `dates_cooked` field.
4. Optionally edit extra meal history and reviews in `data/meal_log.csv`.
5. Rebuild the site data:

```sh
Rscript scripts/build-recipes.R
```

6. Open the site locally or publish it with GitHub Pages.

## Recipe Markdown Format

Each recipe is a Markdown file with YAML-style front matter:

```md
---
title: Lemony Chickpea Rice Bowls
category: Lunch
prep_time: 25 min
servings: 4
ingredients:
  - chickpeas
  - rice
  - spinach
labels:
  - chickpeas
  - rice
tags:
  - meal prep
  - vegetarian
dates_cooked:
  - 2026-07-08
source:
---

## Notes

Recipe notes go here.
```

`ingredients` are the full ingredient list. `labels` are the pantry/filter labels used for matching, so they can be simpler than the full ingredient list. `dates_cooked` feeds the calendar and also appears on each recipe page.

## Data Files

- `recipes/*.md`: one Markdown file per recipe.
- `data/pantry.csv`: pantry inventory.
- `data/meal_log.csv`: optional meals eaten and reviews.
- `data/site-data.json`: generated by R for the website.

## Local Preview

```sh
python3 -m http.server 8001
```

Then open `http://127.0.0.1:8001/`.

## GitHub Pages

Commit the whole folder, including `data/site-data.json`, then enable GitHub Pages from the repository settings. GitHub Pages can serve this static site without a Shiny server.
